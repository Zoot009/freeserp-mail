# Auto-HTTPS reverse proxy for the email-automation platform.
# Caddy fetches + renews a Let's Encrypt certificate for $PLATFORM_DOMAIN
# automatically. Point that domain's DNS A record at this server first.
{$PLATFORM_DOMAIN} {
	encode gzip
	reverse_proxy app:3000
}
